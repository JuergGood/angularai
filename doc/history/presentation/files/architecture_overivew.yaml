graph TD
    subgraph Clients
        Browser[Angular Web App]
        Mobile[Android App]
    end

    subgraph AWS_Cloud [AWS Cloud / VPC]
        ALB{Application Load Balancer}

        subgraph Public_Subnet
            ALB
        end

        subgraph Private_Subnet
            subgraph Docker_Frontend [Docker Container: Frontend]
                Frontend[Nginx / Angular]
            end

            subgraph Docker_Backend [Docker Container: Backend]
                Backend[Spring Boot]
            end

            RDS[(PostgreSQL RDS)]
        end
    end

    Browser -->|HTTPS| ALB
    Mobile -->|HTTPS| ALB

    ALB -->|Port 80| Docker_Frontend
    ALB -->|Port 8080| Docker_Backend

    Backend -->|JDBC| RDS